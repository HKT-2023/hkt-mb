import {IDefaultScreenProps} from './TStack';

export type TExchangeStackParamList = {
  EXCHANGE_SCREEN: Record<string, unknown>;
};

export type IExchangeScreenProps = IDefaultScreenProps<'EXCHANGE_SCREEN'>;
